#!/usr/bin/env bash
set -euo pipefail

repo_root="$(git rev-parse --show-toplevel)"
cd "${repo_root}"

if ! command -v uv >/dev/null 2>&1; then
  echo "pre-commit: uv is required to run Ruff" >&2
  exit 1
fi

echo "pre-commit: running Ruff..."
uv run --locked --extra dev ruff check .
